{
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "type": "module",
  "engines": {
    "node": "^22.14.0",
    "pnpm": "^10.12.1"
  },
  "scripts": {
    "turbo": "[ ! -d node_modules ] && pnpm install; turbo",
    "fix:format": "prettier --write .",
    "fix:lint": "eslint --fix .",
    "install:modules": "[ $CI ] && true || pnpm install",
    "test:format": "run-jest --colors --selectProjects format",
    "test:lint": "run-jest --colors --selectProjects lint",
    "build:idl": "mkdir -p packages/session-idls/src/idl && mkdir -p packages/session-idls/src/types && anchor idl build --program-name session-manager --out-ts packages/session-idls/src/types/session-manager.ts --out packages/session-idls/src/idl/session-manager.json && anchor idl build --program-name example --out-ts packages/session-idls/src/types/example.ts --out packages/session-idls/src/idl/example.json"
  },
  "devDependencies": {
    "@cprussin/eslint-config": "catalog:",
    "@cprussin/jest-config": "catalog:",
    "@cprussin/prettier-config": "catalog:",
    "eslint": "catalog:",
    "jest": "catalog:",
    "prettier": "catalog:",
    "turbo": "catalog:"
  }
}

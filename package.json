{
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "type": "module",
  "engines": {
    "node": "^22.14.0",
    "pnpm": "^10.12.1"
  },
  "scripts": {
    "turbo": "[ ! -d node_modules ] && pnpm install; turbo",
    "fix:format": "prettier --write .",
    "fix:lint": "eslint --fix .",
    "install:modules": "[ $CI ] && true || pnpm install",
    "test:format": "run-jest --colors --selectProjects format",
    "test:lint": "run-jest --colors --selectProjects lint",
    "build:idl": "mkdir -p apps/sessions-demo/src/idl &&anchor idl build --program-name session-manager --out-ts apps/sessions-demo/src/idl/session-manager.ts --out apps/sessions-demo/src/idl/session-manager.json && anchor idl build --program-name example --out-ts apps/sessions-demo/src/idl/example.ts --out apps/sessions-demo/src/idl/example.json"
  },
  "devDependencies": {
    "@cprussin/eslint-config": "catalog:",
    "@cprussin/jest-config": "catalog:",
    "@cprussin/prettier-config": "catalog:",
    "eslint": "catalog:",
    "jest": "catalog:",
    "prettier": "catalog:",
    "turbo": "catalog:"
  }
}

[package]
name = "mock-ntt-executor"
version = "0.1.0"
edition = {workspace = true}

[lib]
crate-type = ["cdylib", "lib"]
name = "mock_ntt_executor"

[dependencies]
anchor-lang = { workspace = true, features = ["init-if-needed"] }

[features]
no-entrypoint = []

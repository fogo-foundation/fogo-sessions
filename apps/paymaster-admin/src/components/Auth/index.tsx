"use client";
import { SessionButton } from "@fogo/sessions-sdk-react";

export const Auth = () => {
  return (
    <div>
      <h1>Auth</h1>
      this is the auth screen
      <SessionButton />
    </div>
  );
};

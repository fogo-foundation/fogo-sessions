{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": [
        "DISABLE_ACCESSIBILITY_REPORTING",
        "GOOGLE_ANALYTICS_ID",
        "VERCEL_ENV",
        "DATABASE_URL"
      ]
    },
    "test:types": {
      "dependsOn": ["build"]
    }
  }
}
